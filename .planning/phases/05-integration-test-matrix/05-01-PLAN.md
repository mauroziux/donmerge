---
phase: 05-integration-test-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/workflows/code-review/__tests__/processor-integration.test.ts",
  "src/workflows/code-review/__tests__/fixtures/pr-run-1-two-issues.json",
  "src/workflows/code-review/__tests__/fixtures/pr-run-2-partial-fix.json",
  "src/workflows/code-review/__tests__/fixtures/pr-run-3-reintroduced.json",
  ".planning/ISSUE-TRACKING-PLAN.md"
]
autonomous: true
requirements: ["TEST-01", "TEST-02", "TEST-03"]
user_setup: []

must_haves:
  truths:
    - "All 7 core lifecycle scenarios from the original plan are represented in the test matrix"
    - "Integration tests use mocks for GitHub API and Durable Object storage"
    - "Fixture-based replay tests simulate multiple sequential runs of the same PR"
    - "Tests verify no duplicate comments, correct Fixed! replies, and proper state transitions"
  artifacts:
    - path: "src/workflows/code-review/__tests__/processor-integration.test.ts"
      provides: "Main integration test suite with test matrix"
      min_lines: 250
    - path: "src/workflows/code-review/__tests__/fixtures/"
      provides: "JSON fixtures representing different PR states across runs"
    - path: ".planning/ISSUE-TRACKING-PLAN.md"
      provides: "Updated test matrix section with exact test cases"
  key_links:
    - from: "processor-integration.test.ts"
      to: "helpers.ts"
      via: "createTrackedIssue, createReviewCommentPayload"
      pattern: "import.*helpers"
    - from: "processor-integration.test.ts"
      to: "github-api.ts"
      via: "mocked resolveFixedComments and publishReview"
      pattern: "vi\.mock|mocked"
---

<objective>
Design and implement an exact integration test matrix for the DonMerge tracked issue lifecycle.

Purpose: Prevent regressions in duplicate comment prevention, partial fixes, and reintroduction detection by having deterministic, repeatable tests that simulate full PR review cycles.

Output: A comprehensive test file using fixtures and mocks that covers all major scenarios, plus updates to the master plan.
</objective>

<execution_context>
@/Users/mauriciosuarez/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ISSUE-TRACKING-PLAN.md
@src/workflows/code-review/__tests__/helpers.ts
@src/workflows/code-review/processor.ts
@src/workflows/code-review/github-api.ts
@src/workflows/code-review/issue-matcher.ts
@src/workflows/code-review/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update master plan with detailed test matrix</name>
  <files>.planning/ISSUE-TRACKING-PLAN.md</files>
  <action>Update the Phase 5 section with a precise test matrix table that includes input conditions, expected outputs, and assertions for each of the 7 scenarios plus 3 additional edge cases (metadata parsing failure, line movement, wording change). Make the matrix very specific with exact expected values for newIssues, resolvedIssues, reintroducedIssues, comments posted, replies posted.</action>
  <verify>grep -A 30 "Test Matrix" .planning/ISSUE-TRACKING-PLAN.md</verify>
  <done>Phase 5 now contains a detailed test matrix with inputs/outputs for every scenario</done>
</task>

<task type="auto">
  <name>Task 2: Create fixture files for replay testing</name>
  <files>src/workflows/code-review/__tests__/fixtures/pr-run-1-two-issues.json, src/workflows/code-review/__tests__/fixtures/pr-run-2-partial-fix.json, src/workflows/code-review/__tests__/fixtures/pr-run-3-reintroduced.json</files>
  <action>Create three JSON fixtures representing sequential runs of the same PR:
  - Run 1: Two new issues detected
  - Run 2: One issue fixed, one persists
  - Run 3: The fixed issue is reintroduced
  Each fixture should contain the LLM result (lineComments) and expected match result structure.</action>
  <verify>ls src/workflows/code-review/__tests__/fixtures/</verify>
  <done>Three fixture files exist with realistic test data for lifecycle replay</done>
</task>

<task type="auto">
  <name>Task 3: Implement integration test suite with full matrix</name>
  <files>src/workflows/code-review/__tests__/processor-integration.test.ts</files>
  <action>Create a new test file that:
  - Imports all necessary modules and helpers
  - Sets up vi.mock for github-api functions (publishReview, resolveFixedComments)
  - Uses the fixture replay pattern to test the full lifecycle across multiple runs
  - Implements at least 8 test cases covering the full matrix from the plan
  - Asserts on number of comments posted, replies posted, final tracked issue states, and no duplicates
  - Uses the existing helpers.createTrackedIssue() extensively
  Follow the pattern from processor-utils.test.ts for mocking.</action>
  <verify>npm run test -- src/workflows/code-review/__tests__/processor-integration.test.ts --passWithNoTests</verify>
  <done>Integration test file exists with comprehensive test matrix covering all key lifecycle scenarios</done>
</task>

</tasks>

<verification>
Run the new integration tests and ensure all matrix scenarios pass with mocked GitHub API.
</verification>

<success_criteria>
- Detailed test matrix documented in ISSUE-TRACKING-PLAN.md
- Fixture-based replay tests implemented
- All 7+ scenarios from the original plan are covered with automated assertions
- Tests run and pass using Vitest with mocks (no real GitHub calls)
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-test-matrix/05-01-SUMMARY.md` summarizing the test matrix and coverage achieved.
</output>
